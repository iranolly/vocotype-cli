sounddevice==0.5.2
keyboard==0.13.5
pyperclip==1.11.0
librosa==0.11.0
soundfile==0.13.1
funasr_onnx==0.4.1
jieba==0.42.1

# Volcengine BigASR 流式识别（backend == "volcengine" 时需要）
websockets>=12.0

# Optional backends for model downloading (recommended)
# 选其一或都装：用于 AutoModel 拉取/缓存模型
modelscope==1.30.0

# 运行时依赖（实际 import 需要）
pypinyin==0.55.0
pystray==0.19.5
pynput==1.8.2
pillow==12.2.0

# GPU 依赖（PyTorch + CUDA 12.6）
torch==2.12.0+cu126
torchaudio==2.11.0+cu126
funasr==1.3.2

# 说明：
# - ONNX 路线：onnxruntime 已由 funasr_onnx 依赖引入
# - GPU 路线：torch(+cu) + torchaudio + funasr

